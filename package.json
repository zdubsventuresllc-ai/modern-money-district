{
  "name": "modern-money-district",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Walkable Stabledash city block + Agent Pay Arcade (lab spike)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "start": "serve dist -l tcp://0.0.0.0:${PORT:-4173}"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "serve": "^14.2.4",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
